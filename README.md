para rodar o dashboard é necessário baixar umas pendências 

Pip install plotly e scipy
pip install streamlit

para rodar basta colocar este comando 
streamlit run dashboard.py
caso o camndo não rode (motivos de estar no windows, e acusar problemas de path) rode este aqui 
 python -m streamlit run dashboard.py
